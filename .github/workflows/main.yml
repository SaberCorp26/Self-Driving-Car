name: Deploy on commit to main
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v7
      - name: Deploy
        run: |
          docker compose up -d --build
          docker image prune -f
